"""Migration: Add Order Management Plugin TablesCreates database tables for the sales_order_management plugin:  - orders  - order_items"""import loggingfrom config.database import Base, sync_enginelogger = logging.getLogger(__name__)def migrate_add_order_management_tables():    """Create Order Management plugin tables in the database."""    logger.info("=ƒöä Running Order Management tables migration...")    if sync_engine is None:        logger.warning(            "GÜán+Å Sync database engine not available, skipping order management tables migration"        )        return    try:        import models  # noqa: F401 GÇö registers all models with Base        from models.order import (            Order,            OrderItem,            InventoryItem,        )        Base.metadata.create_all(            bind=sync_engine,            tables=[                Order.__table__,                OrderItem.__table__,                InventoryItem.__table__,            ],        )        logger.info("G£à Order Management tables migration completed successfully")    except Exception as e:        logger.error(f"G¥î Order Management tables migration failed: {e}")        raiseif __name__ == "__main__":    logging.basicConfig(level=logging.INFO)    migrate_add_order_management_tables()