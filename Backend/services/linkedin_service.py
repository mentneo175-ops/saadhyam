"""LinkedIn Service for Store SolutionArchitecture:- Zero .env dependency: OAuth Application credentials stored encrypted in LinkedInPluginConfig (scoped to plugin_key="marketing_linkedin")- Multi-tenant user isolation: Scoped strictly to (user_id, plugin_key="marketing_linkedin")- Encrypted OAuth tokens at rest (AES/Fernet)- OpenID Connect user profile retrieval- Official LinkedIn REST Posts API publishing (POST https://api.linkedin.com/rest/posts)- Zero sensitive credentials logged or exposed to frontend"""import loggingimport urllib.parsefrom datetime import datetime, timedeltafrom typing import Dict, Any, Optional, Tuple, Listimport httpxfrom jose import jwt, JWTErrorfrom sqlalchemy import select, descfrom sqlalchemy.ext.asyncio import AsyncSessionfrom config.settings import settingsfrom models.linkedin import (    LinkedInPluginConfig,    LinkedInConnection,    LinkedInPostHistory,    LinkedInPostStatus,)from services.encryption_service import get_encryption_servicelogger = logging.getLogger(__name__)encryption_service = get_encryption_service()# ConstantsSTORE_PLUGIN_KEY = "marketing_linkedin"DEFAULT_REDIRECT_URI = "http://localhost:8000/api/linkedin/oauth/callback"LINKEDIN_AUTH_URL = "https://www.linkedin.com/oauth/v2/authorization"LINKEDIN_TOKEN_URL = "https://www.linkedin.com/oauth/v2/accessToken"LINKEDIN_USERINFO_URL = "https://api.linkedin.com/v2/userinfo"LINKEDIN_POSTS_API_URL = "https://api.linkedin.com/rest/posts"LINKEDIN_API_VERSION = "202401"LINKEDIN_OAUTH_SCOPES = "openid profile email w_member_social"class LinkedInService:    """Enterprise-grade service for user-isolated LinkedIn integration with database-backed OAuth configuration."""    @classmethod    async def get_plugin_credentials(cls, db: AsyncSession) -> Tuple[Optional[str], Optional[str], str]:        """        Retrieve and decrypt LinkedIn OAuth application client credentials from the database for marketing_linkedin.        Returns (client_id, client_secret, redirect_uri) or (None, None, default_redirect_uri).        """        try:            stmt = select(LinkedInPluginConfig).where(LinkedInPluginConfig.plugin_key == STORE_PLUGIN_KEY)            result = await db.execute(stmt)            config = result.scalar_one_or_none()            if not config or not config.is_active or not config.client_id:                return None, None, DEFAULT_REDIRECT_URI            client_id = config.client_id.strip() if config.client_id else None            redirect_uri = config.redirect_uri.strip() if config.redirect_uri else DEFAULT_REDIRECT_URI            client_secret = None            if config.client_secret_encrypted:                try:                    client_secret = encryption_service.decrypt(config.client_secret_encrypted)                except Exception as e:                    logger.error(f"Failed to decrypt LinkedIn client secret: {e}")                    client_secret = None            return client_id, client_secret, redirect_uri        except Exception as e:            logger.error(f"Error querying LinkedIn plugin configuration: {e}")            return None, None, DEFAULT_REDIRECT_URI    @classmethod    async def set_plugin_config(        cls,        client_id: str,        client_secret: str,        db: AsyncSession,        redirect_uri: Optional[str] = None,        is_active: bool = True,    ) -> Dict[str, Any]:        """        Securely save or update OAuth application configuration for marketing_linkedin.        Encrypts client_secret at rest.        """        stmt = select(LinkedInPluginConfig).where(LinkedInPluginConfig.plugin_key == STORE_PLUGIN_KEY)        result = await db.execute(stmt)        config = result.scalar_one_or_none()        enc_secret = encryption_service.encrypt(client_secret.strip()) if client_secret else None        target_redirect = (redirect_uri or DEFAULT_REDIRECT_URI).strip()        now = datetime.utcnow()        if config:            config.client_id = client_id.strip()            if enc_secret:                config.client_secret_encrypted = enc_secret            config.redirect_uri = target_redirect            config.is_active = is_active            config.updated_at = now        else:            config = LinkedInPluginConfig(                plugin_key=STORE_PLUGIN_KEY,                client_id=client_id.strip(),                client_secret_encrypted=enc_secret,                redirect_uri=target_redirect,                is_active=is_active,                created_at=now,                updated_at=now,            )            db.add(config)        await db.commit()        await db.refresh(config)        logger.info(f"G£à LinkedIn plugin OAuth application configuration saved for '{STORE_PLUGIN_KEY}'")        return {            "success": True,            "message": "LinkedIn OAuth application configuration saved successfully.",            "configured": True,            "client_id": config.client_id,            "redirect_uri": config.redirect_uri,            "is_active": config.is_active,        }    @classmethod    async def get_plugin_config_status(cls, db: AsyncSession) -> Dict[str, Any]:        """Get safe plugin configuration status without leaking the client secret."""        stmt = select(LinkedInPluginConfig).where(LinkedInPluginConfig.plugin_key == STORE_PLUGIN_KEY)        result = await db.execute(stmt)        config = result.scalar_one_or_none()        if not config:            return {                "configured": False,                "plugin_key": STORE_PLUGIN_KEY,                "client_id": None,                "redirect_uri": DEFAULT_REDIRECT_URI,                "is_active": False,                "is_secret_set": False,                "updated_at": None,            }        return {            "configured": bool(config.client_id and config.client_secret_encrypted),            "plugin_key": STORE_PLUGIN_KEY,            "client_id": config.client_id,            "redirect_uri": config.redirect_uri,            "is_active": config.is_active,            "is_secret_set": bool(config.client_secret_encrypted),            "updated_at": config.updated_at.isoformat() if config.updated_at else None,        }    @staticmethod    def generate_oauth_state(user_id: int) -> str:        """Generate a signed, short-lived (15 min) JWT state token containing user_id and plugin_key."""        payload = {            "user_id": user_id,            "plugin_key": STORE_PLUGIN_KEY,            "purpose": "linkedin_oauth",            "exp": datetime.utcnow() + timedelta(minutes=15),            "iat": datetime.utcnow(),        }        return jwt.encode(payload, settings.SECRET_KEY, algorithm=settings.ALGORITHM)    @staticmethod    def verify_oauth_state(state: str) -> Optional[int]:        """Verify signed OAuth state token and return user_id if valid."""        if not state:            return None        try:            payload = jwt.decode(state, settings.SECRET_KEY, algorithms=[settings.ALGORITHM])            if payload.get("purpose") != "linkedin_oauth":                logger.warning("Invalid LinkedIn OAuth state purpose")                return None            if payload.get("plugin_key") != STORE_PLUGIN_KEY:                logger.warning("Invalid LinkedIn OAuth state plugin_key")                return None            return payload.get("user_id")        except (JWTError, Exception) as e:            logger.warning(f"Failed to verify LinkedIn OAuth state: {e}")            return None    @classmethod    async def get_authorization_url(cls, user_id: int, db: AsyncSession) -> Dict[str, Any]:        """Generate official LinkedIn OAuth 2.0 authorization URL for authenticated user using database config."""        client_id, _, redirect_uri = await cls.get_plugin_credentials(db)        if not client_id:            logger.warning(f"LinkedIn OAuth application is not configured for {STORE_PLUGIN_KEY}")            return {                "success": False,                "error": "CONFIG_ERROR",                "message": "LinkedIn OAuth Application is not configured for marketing_linkedin. Please configure Client ID and Client Secret.",                "auth_url": "",            }        state = cls.generate_oauth_state(user_id)        params = {            "response_type": "code",            "client_id": client_id,            "redirect_uri": redirect_uri,            "state": state,            "scope": LINKEDIN_OAUTH_SCOPES,        }        query_string = urllib.parse.urlencode(params)        auth_url = f"{LINKEDIN_AUTH_URL}?{query_string}"        return {            "success": True,            "auth_url": auth_url,            "state": state,        }    @classmethod    async def handle_oauth_callback(        cls,        code: str,        state: str,        db: AsyncSession,        expected_user_id: Optional[int] = None,    ) -> Dict[str, Any]:        """        Exchange authorization code for access token and fetch userinfo.        Saves encrypted tokens and member profile in LinkedInConnection for that specific user.        """        user_id = cls.verify_oauth_state(state)        if not user_id:            logger.error("Invalid or expired OAuth state parameter")            return {                "success": False,                "error": "INVALID_STATE",                "message": "OAuth state mismatch or session expired. Please retry connecting.",            }        if expected_user_id and user_id != expected_user_id:            logger.error(f"State user_id={user_id} does not match expected_user_id={expected_user_id}")            return {                "success": False,                "error": "USER_MISMATCH",                "message": "Security verification failed: user identity mismatch.",            }        client_id, client_secret, redirect_uri = await cls.get_plugin_credentials(db)        if not client_id or not client_secret:            logger.error("Missing LinkedIn OAuth credentials in database for marketing_linkedin")            return {                "success": False,                "error": "CONFIG_ERROR",                "message": "LinkedIn OAuth credentials not configured in database for marketing_linkedin.",            }        # 1. Exchange authorization code for token        async with httpx.AsyncClient(timeout=15.0) as client:            try:                token_resp = await client.post(                    LINKEDIN_TOKEN_URL,                    data={                        "grant_type": "authorization_code",                        "code": code,                        "redirect_uri": redirect_uri,                        "client_id": client_id,                        "client_secret": client_secret,                    },                    headers={"Content-Type": "application/x-www-form-urlencoded"},                )                if token_resp.status_code != 200:                    err_body = token_resp.text                    logger.error(f"LinkedIn token exchange failed: {token_resp.status_code} - {err_body}")                    return {                        "success": False,                        "error": "TOKEN_EXCHANGE_FAILED",                        "message": f"LinkedIn returned error during token exchange: {err_body}",                    }                token_data = token_resp.json()            except Exception as e:                logger.error(f"Network error during LinkedIn token exchange: {e}", exc_info=True)                return {                    "success": False,                    "error": "NETWORK_ERROR",                    "message": f"Failed to connect to LinkedIn authorization server: {str(e)}",                }            access_token = token_data.get("access_token")            expires_in = token_data.get("expires_in", 5184000)  # Default ~60 days            refresh_token = token_data.get("refresh_token")            scopes = token_data.get("scope", LINKEDIN_OAUTH_SCOPES)            if not access_token:                logger.error("No access_token returned in LinkedIn token response")                return {                    "success": False,                    "error": "NO_ACCESS_TOKEN",                    "message": "LinkedIn did not return a valid access token.",                }            # 2. Retrieve Authenticated Member Identity (OpenID Connect userinfo)            member_id = None            member_name = None            member_email = None            member_picture = None            try:                userinfo_resp = await client.get(                    LINKEDIN_USERINFO_URL,                    headers={"Authorization": f"Bearer {access_token}"},                )                if userinfo_resp.status_code == 200:                    uinfo = userinfo_resp.json()                    member_id = uinfo.get("sub")                    member_name = uinfo.get("name")                    member_email = uinfo.get("email")                    member_picture = uinfo.get("picture")                else:                    logger.warning(f"Could not retrieve userinfo: {userinfo_resp.status_code} {userinfo_resp.text}")            except Exception as e:                logger.warning(f"Error fetching LinkedIn userinfo: {e}")            # Fallback member name if userinfo failed            if not member_name:                member_name = f"LinkedIn User ({member_id or 'Connected'})"        # 3. Encrypt tokens at rest and persist in database scoped to (user_id, plugin_key)        enc_access_token = encryption_service.encrypt(access_token)        enc_refresh_token = encryption_service.encrypt(refresh_token) if refresh_token else None        expires_at = datetime.utcnow() + timedelta(seconds=int(expires_in))        # Check existing connection for this user and store plugin        stmt = select(LinkedInConnection).where(            LinkedInConnection.user_id == user_id,            LinkedInConnection.plugin_key == STORE_PLUGIN_KEY,        )        result = await db.execute(stmt)        conn = result.scalar_one_or_none()        now = datetime.utcnow()        if conn:            conn.linkedin_member_id = member_id or conn.linkedin_member_id            conn.linkedin_name = member_name or conn.linkedin_name            conn.linkedin_email = member_email or conn.linkedin_email            conn.linkedin_profile_picture = member_picture or conn.linkedin_profile_picture            conn.access_token_encrypted = enc_access_token            conn.refresh_token_encrypted = enc_refresh_token or conn.refresh_token_encrypted            conn.token_expires_at = expires_at            conn.scopes = scopes            conn.is_active = True            conn.last_connected_at = now            conn.updated_at = now        else:            conn = LinkedInConnection(                user_id=user_id,                plugin_key=STORE_PLUGIN_KEY,                linkedin_member_id=member_id,                linkedin_name=member_name,                linkedin_email=member_email,                linkedin_profile_picture=member_picture,                access_token_encrypted=enc_access_token,                refresh_token_encrypted=enc_refresh_token,                token_expires_at=expires_at,                scopes=scopes,                is_active=True,                last_connected_at=now,            )            db.add(conn)        await db.commit()        await db.refresh(conn)        logger.info(f"G£à LinkedIn connection established for user_id={user_id}, member={member_name}")        return {            "success": True,            "message": "LinkedIn account connected successfully.",            "user_id": user_id,            "member_id": member_id,            "member_name": member_name,            "member_email": member_email,        }    @classmethod    async def get_connection(cls, user_id: int, db: AsyncSession) -> Optional[LinkedInConnection]:        """Fetch LinkedIn connection scoped strictly to user_id and store plugin_key."""        stmt = select(LinkedInConnection).where(            LinkedInConnection.user_id == user_id,            LinkedInConnection.plugin_key == STORE_PLUGIN_KEY,        )        result = await db.execute(stmt)        return result.scalar_one_or_none()    @classmethod    async def get_connection_status(cls, user_id: int, db: AsyncSession) -> Dict[str, Any]:        """Get safe connection status dictionary for authenticated user without leaking tokens."""        conn = await cls.get_connection(user_id, db)        if not conn or not conn.is_active or not conn.access_token_encrypted:            return {                "connected": False,                "is_active": False,                "member_name": None,                "member_email": None,                "member_id": None,                "profile_picture": None,                "connected_at": None,                "expires_at": None,                "is_expired": False,            }        is_expired = False        if conn.token_expires_at and conn.token_expires_at < datetime.utcnow():            is_expired = True        return {            "connected": conn.is_active and not is_expired,            "is_active": conn.is_active,            "member_name": conn.linkedin_name,            "member_email": conn.linkedin_email,            "member_id": conn.linkedin_member_id,            "profile_picture": conn.linkedin_profile_picture,            "connected_at": conn.last_connected_at.isoformat() if conn.last_connected_at else None,            "expires_at": conn.token_expires_at.isoformat() if conn.token_expires_at else None,            "is_expired": is_expired,        }    @classmethod    async def get_valid_access_token(cls, user_id: int, db: AsyncSession) -> Tuple[Optional[str], Optional[str], Optional[LinkedInConnection]]:        """        Decrypt and validate access token for authenticated user.        Returns (access_token, member_id, connection) or (None, None, None).        """        conn = await cls.get_connection(user_id, db)        if not conn or not conn.is_active or not conn.access_token_encrypted:            return None, None, None        if conn.token_expires_at and conn.token_expires_at < datetime.utcnow():            logger.warning(f"LinkedIn token expired for user_id={user_id}")            conn.is_active = False            await db.commit()            return None, None, conn        try:            token = encryption_service.decrypt(conn.access_token_encrypted)            return token, conn.linkedin_member_id, conn        except Exception as e:            logger.error(f"Failed to decrypt LinkedIn access token: {e}")            return None, None, conn    @classmethod    async def publish_text_post(        cls,        user_id: int,        content: str,        db: AsyncSession,        topic: Optional[str] = None,    ) -> Dict[str, Any]:        """        Publish post directly to member's LinkedIn feed using official Posts REST API.        Enforces user isolation and records post under the authenticated user.        POST https://api.linkedin.com/rest/posts        """        if not content or not content.strip():            return {                "success": False,                "error": "EMPTY_CONTENT",                "message": "Post content cannot be empty.",            }        access_token, member_id, conn = await cls.get_valid_access_token(user_id, db)        if not access_token or not conn:            return {                "success": False,                "error": "NOT_CONNECTED",                "message": "LinkedIn account not connected or access token has expired. Please reconnect LinkedIn.",            }        # Create initial pending record in history scoped to user and store product        history_item = LinkedInPostHistory(            user_id=user_id,            plugin_key=STORE_PLUGIN_KEY,            linkedin_connection_id=conn.id,            content=content.strip(),            topic=topic or "LinkedIn Post",            status=LinkedInPostStatus.PUBLISHING,        )        db.add(history_item)        await db.commit()        await db.refresh(history_item)        # Build LinkedIn Posts API payload        author_urn = f"urn:li:person:{member_id}" if member_id else f"urn:li:person:{conn.linkedin_member_id}"        payload = {            "author": author_urn,            "commentary": content.strip(),            "visibility": "PUBLIC",            "distribution": {                "feedDistribution": "MAIN_FEED",                "targetEntities": [],                "thirdPartyDistributionChannels": [],            },            "lifecycleState": "PUBLISHED",            "isReshareDisabledByAuthor": False,        }        headers = {            "Authorization": f"Bearer {access_token}",            "LinkedIn-Version": LINKEDIN_API_VERSION,            "X-Restli-Protocol-Version": "2.0.0",            "Content-Type": "application/json",        }        async with httpx.AsyncClient(timeout=20.0) as client:            try:                response = await client.post(LINKEDIN_POSTS_API_URL, json=payload, headers=headers)                status_code = response.status_code                if status_code in (200, 201):                    post_urn = response.headers.get("x-restli-id") or ""                    if not post_urn:                        try:                            resp_json = response.json()                            post_urn = resp_json.get("id", "")                        except Exception:                            post_urn = f"urn:li:share:{history_item.id}"                    history_item.post_urn = post_urn                    history_item.status = LinkedInPostStatus.PUBLISHED                    history_item.published_at = datetime.utcnow()                    history_item.error_message = None                    await db.commit()                    await db.refresh(history_item)                    logger.info(f"G£à LinkedIn post published successfully! URN: {post_urn} for user_id={user_id}")                    return {                        "success": True,                        "message": "Post published to LinkedIn successfully!",                        "post_urn": post_urn,                        "post_id": history_item.id,                        "published_at": history_item.published_at.isoformat() if history_item.published_at else None,                    }                elif status_code == 401:                    logger.error(f"LinkedIn 401 Unauthorized for user_id={user_id}. Marking connection inactive.")                    conn.is_active = False                    history_item.status = LinkedInPostStatus.FAILED                    history_item.error_message = "LinkedIn connection expired or unauthorized (401). Please reconnect."                    await db.commit()                    return {                        "success": False,                        "error": "UNAUTHORIZED",                        "message": "LinkedIn connection expired. Please reconnect your LinkedIn account.",                    }                elif status_code == 403:                    err_text = response.text                    logger.error(f"LinkedIn 403 Forbidden for user_id={user_id}: {err_text}")                    history_item.status = LinkedInPostStatus.FAILED                    history_item.error_message = f"Permission denied (403): {err_text}"                    await db.commit()                    return {                        "success": False,                        "error": "FORBIDDEN",                        "message": "LinkedIn permission denied (403). Make sure your LinkedIn App has 'w_member_social' permission.",                    }                elif status_code == 429:                    logger.warning(f"LinkedIn 429 Rate Limit for user_id={user_id}")                    history_item.status = LinkedInPostStatus.FAILED                    history_item.error_message = "LinkedIn API rate limit reached (429)."                    await db.commit()                    return {                        "success": False,                        "error": "RATE_LIMITED",                        "message": "LinkedIn rate limit reached. Please wait a few minutes before posting again.",                    }                else:                    err_text = response.text                    logger.error(f"LinkedIn API error {status_code} for user_id={user_id}: {err_text}")                    history_item.status = LinkedInPostStatus.FAILED                    history_item.error_message = f"LinkedIn API error ({status_code}): {err_text}"                    await db.commit()                    return {                        "success": False,                        "error": f"LINKEDIN_API_ERROR_{status_code}",                        "message": f"LinkedIn rejected post ({status_code}): {err_text}",                    }            except httpx.TimeoutException:                logger.error(f"Timeout while posting to LinkedIn for user_id={user_id}")                history_item.status = LinkedInPostStatus.FAILED                history_item.error_message = "Request timed out while communicating with LinkedIn."                await db.commit()                return {                    "success": False,                    "error": "TIMEOUT",                    "message": "LinkedIn API request timed out. Please check your network connection and try again.",                }            except Exception as e:                logger.error(f"Unexpected error publishing to LinkedIn for user_id={user_id}: {e}", exc_info=True)                history_item.status = LinkedInPostStatus.FAILED                history_item.error_message = str(e)                await db.commit()                return {                    "success": False,                    "error": "UNEXPECTED_ERROR",                    "message": f"Failed to publish post: {str(e)}",                }    @classmethod    async def get_post_history(        cls,        user_id: int,        db: AsyncSession,        limit: int = 50,    ) -> List[Dict[str, Any]]:        """Retrieve recent LinkedIn post history scoped strictly to authenticated user and store product."""        stmt = (            select(LinkedInPostHistory)            .where(                LinkedInPostHistory.user_id == user_id,                LinkedInPostHistory.plugin_key == STORE_PLUGIN_KEY,            )            .order_by(desc(LinkedInPostHistory.created_at))            .limit(limit)        )        result = await db.execute(stmt)        posts = result.scalars().all()        return [            {                "id": p.id,                "post_urn": p.post_urn,                "content": p.content,                "topic": p.topic,                "status": p.status.value if hasattr(p.status, "value") else str(p.status),                "error_message": p.error_message,                "published_at": p.published_at.isoformat() if p.published_at else None,                "created_at": p.created_at.isoformat() if p.created_at else None,            }            for p in posts        ]    @classmethod    async def disconnect(cls, user_id: int, db: AsyncSession) -> Dict[str, Any]:        """Disconnect LinkedIn account and revoke active tokens strictly for the authenticated user."""        conn = await cls.get_connection(user_id, db)        if conn:            conn.is_active = False            conn.access_token_encrypted = None            conn.refresh_token_encrypted = None            conn.updated_at = datetime.utcnow()            await db.commit()            logger.info(f"LinkedIn disconnected for user_id={user_id}")        return {            "success": True,            "message": "LinkedIn account disconnected successfully.",        }